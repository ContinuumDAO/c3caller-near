{
  "name": "c3caller",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/c3caller.ts build/c3caller.wasm",
    "test": "$npm_execpath run build && ava -- ./build/c3caller.wasm"
  },
  "dependencies": {
    "ethers": "^6.13.2",
    "near-sdk-js": "^2.0.0"
  },
  "devDependencies": {
    "ava": "^6.1.3",
    "near-workspaces": "^3.5.0",
    "typescript": "^5.4.5"
  },
  "ava": {
    "timeout": 50000,
    "files": [
      "sandbox-test/*.ava.js"
    ]
  }
}
